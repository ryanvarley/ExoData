language: python

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

install: "pip install -r requirements.txt --use-mirrors"

python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"

script:
  - python setup.py test

# blacklist
branches:
  except:
    - images